# URL of the .NET simulation engine (used server-side by Next.js rewrites)
ENGINE_URL=http://localhost:5050

# Exposed to the browser (prefix NEXT_PUBLIC_)
NEXT_PUBLIC_ENGINE_URL=http://localhost:5050
